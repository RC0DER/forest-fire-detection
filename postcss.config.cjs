// Minimal PostCSS config to ensure Vite can load PostCSS without searching
// for unexpected/malformed JSON configs. Add plugins here if/when needed.
module.exports = {
  plugins: {
    // Example: 'autoprefixer': {}
  }
};
